export default async function Album({ params }: { params: { albumId: string } }) {

  const { albumId } = params;

  // fetch resources for albumId

  // sort resources:
  // 

  return (
    <div>
      album
    </div>
  )
}